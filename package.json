{
  "name": "codeprobe",
  "version": "0.1.0",
  "description": "Gamified code quiz plugin for Claude Code",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "node --test tests/store.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
